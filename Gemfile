# frozen_string_literal: true
# Used for local preview only — GitHub Pages builds with its own dependency set.

source "https://rubygems.org"

gem "jekyll"
gem "jekyll-seo-tag"
gem "jekyll-sitemap"
gem "jemoji"
gem "kramdown-parser-gfm"

# https://github.com/jekyll/jekyll/issues/8523#issuecomment-751409319
# Avoids `require': cannot load such file -- webrick (LoadError) when serving locally
gem "webrick"
